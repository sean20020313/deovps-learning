# 第一部分 基本語法

## 可以使用python3 REPL互動式命令可以不用每次都建立py檔案
### python3進入  exit()退出

## 執行方式
### chmod +x test.py ./test.py

## python 程式碼區塊是透過行縮縮排來表示

## python 以\來表示換行符號
### total = item_one + \


## 單行多語句
### import sys; x = 'foo'; sys.stdout.wrte(x + '\n')

